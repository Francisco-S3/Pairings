import 'dart:convert';
import 'dart:io';

// utility to call signinController for user authentication
bool signinController(String email, String password) {
  //bool signinController(String email, String password) async {


    // ***** STUB OUT FOR CONTROLLER CALL TO AUTHENTICATION *****

    // Map data = {
    //   'email' : email,
    //   'password' : password
    // };
    // var jsonData = null;
    // SharedPreferences sharedPreferences = await SharedPreferences.getInstance();
    // var response = await ??();
    // if(response == ??) {
    //  jsonData = jsonDecode(???)
    // }

    return true;
}